# Snip CLI

Zero-dependency Node CLI for the Snip backend.

## Commands

- `snip add <url>`
- `snip ls`
- `snip open <code>`
- `snip help`

## Environment

- `SNIP_API` (default `http://localhost:3000`)
